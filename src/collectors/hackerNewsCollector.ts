import type { Collector } from "./types";

export const collectHackerNewsItems: Collector = async () => {
  // TODO: Use Hacker News as a discovery signal, not a final source of truth.
  return [];
};
