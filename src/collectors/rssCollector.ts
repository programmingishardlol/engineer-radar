import type { Collector } from "./types";

export const collectRssItems: Collector = async () => {
  // TODO: Implement RSS fetching after mock vertical slice and persistence are stable.
  return [];
};
