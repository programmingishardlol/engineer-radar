import type { SourceRegistryItem } from "../types";

export async function listSources(): Promise<SourceRegistryItem[]> {
  // TODO: Backend + Database Agent will read persisted sources.
  return [];
}
